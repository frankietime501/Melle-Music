

module.exports = {
    token: process.env.TOKEN,
    prefix: process.env.PREFIX || '!',
    clientId: process.env.CLIENT_ID,
    guildId: process.env.GUILD_ID,
    owners: ['1424706436482600962'], // Add bot owner IDs
    colors: {
        background: '#111827',
        accent: '#a855f7',
        success: '#22c55e',
        error: '#ef4444'
    }
};
